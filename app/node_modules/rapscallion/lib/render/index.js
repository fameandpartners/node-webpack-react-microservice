"use strict";

var traverse = require("./traverse");

var _require = require("./context"),
    getRootContext = _require.getRootContext;

function render(seq, node, rootContext) {
  rootContext = rootContext || getRootContext();
  traverse({
    seq,
    node,
    context: rootContext
  });
  return seq;
}

module.exports = render;